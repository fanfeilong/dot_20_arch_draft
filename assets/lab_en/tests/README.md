# tests

This directory will contain the integration tests for the mini implementation in `src/`.

Start with one end-to-end test for the smallest runnable slice.

Add tests only when they clarify the core architecture behavior.
