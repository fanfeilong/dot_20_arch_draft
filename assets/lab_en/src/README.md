# src

This directory will contain the runnable mini implementation derived from the architecture docs.

Use the target project's stack whenever practical.

The first implementation goal is not feature coverage. It is to demonstrate the core architecture idea with the smallest runnable slice.
