# report

This directory will contain the local report application and related assets.

This directory includes a starter Vue skeleton under `vue-app/`.

The local report experience should present:

- the architecture docs
- the mini implementation
- the integration tests
- the final teaching narrative

Starter commands:

```bash
cd .d2a/report/vue-app
npm install
npm run dev
```
